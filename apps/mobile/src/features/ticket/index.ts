/**
 * Billets — liste et détail.
 *
 * Une réservation de trois places produit trois billets, un par passager :
 * la liste est le point d'arrivée après paiement, pas un billet unique.
 */
export { TicketScreen } from './ui/TicketScreen'
export { TicketListScreen } from './ui/TicketListScreen'
